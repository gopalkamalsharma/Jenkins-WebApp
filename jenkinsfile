pipeline{
    agent any
    stages{
        stage('Deployment'){
            steps{
                sh 'ssh ubuntu@3.7.65.6 '''
                copy docker-compose.yaml /home/ubuntu/docker-compose.yaml
                docker-compose down
                docker-compose up -d
                '''
                '
            }
        }
    }
}